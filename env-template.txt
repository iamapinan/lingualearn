# คัดลอกไฟล์นี้ไปเป็น .env แล้วแก้ไขค่าตามความเหมาะสม

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=lingualearn_user
DB_PASSWORD=your_secure_password_here
DB_NAME=lingualearn_db

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3090
NODE_ENV=development

# JWT Authentication
JWT_SECRET=your_jwt_secret_key_change_this_in_production_min_32_characters
JWT_EXPIRES_IN=7d

# Session Configuration
SESSION_SECRET=your_session_secret_key_change_this_in_production

# GracerAI Configuration (Optional)
GRACERAI_API_KEY=
GRACERAI_HOST=

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3090

# Email Configuration (Optional - for future email verification)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@lingualearn.com

